# pinned to the tested 3.x series: a future 4.x may drop re-exports again
# (the select_subnet top-level removal already broke CI once, 2026-07-08)
pandapower>=3.0,<4
numpy>=1.24
pandas>=2.0
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
openpyxl>=3.1
